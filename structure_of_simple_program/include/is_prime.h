#ifndef __IS_PRIME_H_
#define __IS_PRIME_H_

#include <stdlib.h>
#include <stdbool.h>

/* is_prime: Is the function to know if is prime or not */
bool is_prime(unsigned long long n, unsigned int k);


#endif
